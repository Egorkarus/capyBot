services:
  bot:
    build: .
    container_name: capybot
    restart: always
    network_mode: "host"
    env_file:
      - .env
    volumes:
      - ./src:/usr/src/app/src
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
